package config

// ScanDirectives is a split function for a Scanner that returns each directive as a token.
// func ScanDirectives(data []byte, atEOF bool) (advance int, token []byte, err error) {
// 	if atEOF && len(data) == 0 {
// 		return
// 	}
// 	return
// }
